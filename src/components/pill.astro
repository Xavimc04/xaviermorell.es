--- 

const { ball, box, id } = Astro.props

---

<div id={ id } class={ `border text-sm px-2 py-1 rounded-full flex items-center gap-2 ${ box }` }>
    <div class={ `h-3 w-3 rounded-full ${ ball }` }></div>

    <slot />
</div>